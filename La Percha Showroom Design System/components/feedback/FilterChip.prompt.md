Selectable chip for horizontal category/filter rows.

```jsx
<FilterChip selected={cat==='Vestidos'} onClick={() => setCat('Vestidos')}>Vestidos</FilterChip>
```
Selected state fills with ink (charcoal). Scroll a row of these horizontally on mobile.
